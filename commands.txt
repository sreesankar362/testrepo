git init                =initializing git in present folder

git status              = list all untracked files

git add -A              =add All untracked files to staging area

git commit -m "message" = pass changes from staging area to local repository 